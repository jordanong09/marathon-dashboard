[sales_storage]
backend = "supabase"
url = "https://YOUR-PROJECT.supabase.co"
secret_key = "sb_secret_REPLACE_IN_STREAMLIT_SECRETS_ONLY"
# Deployed apps must never save locally (Streamlit Cloud wipes its disk on restart).
require_supabase = true
